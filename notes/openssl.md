# OpenSSL Help Commands #

**Use openssl to connect to server - for testing certificate validity**  
`openssl s_client -connect 192.168.1.2:443`

**Update the certificates loaded for linux**  
`update-ca-certificates`
